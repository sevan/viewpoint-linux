[Unit]
Description=OpenSSH sshd per-connection daemon
Documentation=man:sshd(8) man:sshd_config(5)

[Service]
ExecStart=-/usr/sbin/sshd -i
StandardInput=socket
RuntimeDirectory=sshd
RuntimeDirectoryMode=0755

